# 账户配置界面整合与等待诊断（2026-09-07）

## 本地修复

- 概览原先读取`credentials.okx_configured/simulated_trading`，而运行概览返回`okx/llm`，导致已配置Key被误显示为未配置。现在后端提供兼容字段及统一连接摘要，前端同时兼容旧响应；账户管理链接指向账户中心。
- 三个用途下拉框默认显示实际绑定。刷新时同步已经保存的绑定，不覆盖用户尚未提交的选择；选择无变化不再允许重复保存。后端同绑定请求也是无副作用的幂等操作，不清理决策、不更换绑定版本。
- 账户卡片展示API Key首尾脱敏值；Secret和Passphrase仅返回已保存布尔状态，不返回明文或片段。配置文件中的旧凭据以只读脱敏视图提供，明确当前生效连接仍以上方用途绑定为准。
- 原自动生成名称中的“待核验”后缀在展示层规范化；真实核验状态另列，不修改用户自定义名称或服务器绑定。
- 后台手动平仓权限移至账户中心。修改需明确确认，只设置原有`R20_MANUAL_CLOSE_ENABLED`，不执行平仓、不改账户绑定，也不关闭独立持仓保护。
- CLI检测/安装维护入口移至账户中心，仍要求原确认与超级管理员权限。
- 原“交易账户与标的”改为“交易标的与持仓”，删除旧环境、Key与全局OAuth编辑表单，保留本金基准、标的管理、持仓读取及受保护的手动平仓。旧配置API暂留兼容，不提供第二套编辑UI。

## 资讯调度

当前网关`JOBS`定义新闻任务间隔600秒。账户中心显示同一调度常量，而不是独立维护另一个周期。服务器最近自动任务记录为16:19、16:29、16:39，符合约每10分钟尝试一次。人工读取可以提前更新；接口成功读取时间不同于新闻发布时间，失败不会推进最近成功时间。

## 连续等待的真实快照

只读核查截至2026-09-07 16:45一轮，计数已经从用户看到的31增加到32。分析窗口08:45～16:45，最近32轮共160条标的决策：

| 状态 | 条数 |
|---|---:|
| WAIT审计通过 | 103 |
| WAIT审计不完整 | 57 |

不完整原因：缺少前轮复查说明26条，盈亏比不足却缺少可计算方案23条，持仓约束与实际仓位不符6条，缺少持仓引用1条，缺少相对前轮变化的新证据1条。

36条决策至少一个方向的重审条件已满足，但仍为WAIT。重审触发不等于入场授权，不能把这36条都当作错失盈利交易。最新一轮5条均通过审计，但这也不证明等待有收益优势；字段引用核验不等于完整的语义或经济有效性验证。

服务器仍按原15分钟LLM策略运行。上次发布的是影子候选/回放工具，没有启用新场景自动交易。因此不能把当前持续等待解释成新入场方案已经实际运行并验证成功，也不能把概览“未配置”的显示错误当作底层没有Key或停止工作的证据。

本轮不改入场门槛、杠杆、资金预算或交易开关。后续仍需解决模型输出契约稳定性、候选提前构造、触发后的明确复核和有效反证，并用新样本验证收益，不以固定等待轮数触发强制交易。

诊断原始快照保留在忽略的`logs/ACCOUNT_UI_WAIT_20260907.json`，摘要在`logs/WAIT_32_DIAGNOSIS_20260907.json`，不随Git公开账户数据。
