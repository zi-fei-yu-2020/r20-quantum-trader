# 复盘展示与入场评分一致性修复

## 服务器核对结果

- 9 月 7 日 20:00 的 `self_improvement` 网关任务成功，20:00:15 写出 `self_improvement_review.md`。
- 最新报告覆盖 15 条平仓记录，模型建议为 `NO_CHANGE`，无新增记忆候选。运行记忆原文件继续保留 9 月 6 日的时间并不表示复盘任务未执行。
- 前台只展示 `AI_TRADING_MEMORY.md`，却声称随复盘更新；没有展示新报告、候选审核与任务状态。旧页面还有硬编码的 35/30/25/10 权重和“最优稳态”描述。
- 取样时连续无候选计数为 61。最近抽取的 16 轮共 80 条记录均是 WAIT：62 条 audited_wait，18 条 incomplete（10 条缺少前轮复查，8 条盈亏比拒绝缺少可计算方案）。该样本并非 80 个开仓候选被分数线拦截。
- 默认 `02_confidence_gatekeeper.py` 确实仍将未校准 confidence 按 75 分、DOGE 80 分硬拒绝，与基础提示词契约冲突。

## 本地修复

1. 新增只读复盘状态投影，区分最近尝试、最近成功报告、运行记忆更新时间、待审核/未通过建议。
2. 前台策略复盘和后台自进化页复用 `EvolutionReviewPanel`；报告和运行记忆分开，移除虚构的权重与“最优稳态”描述。
3. 模型失败/无效输出不再伪装为成功的 NO_CHANGE，不覆盖上次成功报告或待审核建议，也不将新台账版本标记为已复盘。该版本可在后续重试。
4. 复盘优先使用台账 `net_pnl`，携带交易 ID；“已审阅交易 ID”不冒充某条经验的支持证据。自动复盘仍无权直接覆盖运行记忆、改动风险预算或权重。
5. confidence 插件改为有限数值/范围检查，不按评分大小拒绝候选，不对 DOGE 设置额外评分线。保留数据、证据引用、方向、ADX、成本后盈亏比、最终账户预算、OCO 与独立持仓保护。
6. 提示词明确未估计胜率不等于没有优势；先审查具体候选和失效点，避免“消除全部不确定性才交易”。清除风格层“不确定时等待”的绝对表达，不要求所有周期指标同向。原生限价单仍不能代替尚未出现的指标确认。
7. 保留模型原始动作、原始评分与执行最终动作，便于区分模型主动 WAIT、契约不完整和执行拒绝。评分是未校准诊断值，不是胜率。

## 验证与限制

- 587 项后端测试、64 项前端测试；前端类型检查和生产构建通过。
- 新增矩阵验证：同一合格证据在 BTC/DOGE、0/45/60/70/74.9/75/78/80/100 分下不再因评分而改变准入；高分仍不能绕过伪造证据、数据缺失、反向仓位、原方向/ADX 约束和盈亏比检查。通过准入不是发送订单。
- Windows headed Edge：前台 `/lab`、后台 `/admin/evolution`，390/1440 宽度、亮/暗色、成功/失败状态共 16 组，无页面错误或横向溢出。使用模拟 API，不操作真实账户。
- 用一份预先固定的线上提示词/行情快照调用原模型两次：原提示词与新提示词，各一次、最多 75 秒、无重试挑选，温度 0.2、high。两组都返回 5 WAIT / 0 开仓，未使用未来行情、未发送任何订单。
- 因此不能把“代码门槛冲突已修”解释为“持续 WAIT 已完全解决”，更不能声称回测收益、抓住行情或已验证新策略优势。该对照仅一个快照、每组一次；不能据此证明有无交易优势。
- 上述诊断与隔离验证阶段未修改服务器策略，核对的线上基线为 `5d84281`。正式部署版本以 Git 提交与运行容器的 `R20_BUILD_COMMIT` 为准；上线后需观察完整策略周期，区分模型 WAIT、输出不完整、执行拒绝和实际订单，不能把服务健康等同于策略已验证。

原始诊断、模型对照与浏览器报告保留在被 Git 忽略的 `logs/`，不公开账户样本或凭证。
