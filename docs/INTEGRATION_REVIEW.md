# 模型与 OKX 模拟盘联调记录

日期：2026-09-05。分支：`dev`。未提交、未推送，未修改 `main`。

## 当前结论：配置已生效，完整业务验收尚未完成

- 模型：`gemini-3.8-flash-high`；请求思考参数：`high`。
- Base URL：`https://ai.112102.xyz`；实测使用 OpenAI-compatible `/v1/chat/completions`。
- OKX：`demo`，模拟盘凭据已保存并通过私有 REST 读取验证；本轮未写入实盘凭据，未启用手动平仓。
- 当前实际监听 `8080` 的是 Windows Python/Uvicorn 服务，不是 WSL 服务。
- 本轮重启命令被执行环境策略拒绝，原服务未停止。**后端修复还需要用户重启现有服务才能在当前网页中生效**，不能把源代码测试通过等同于已部署验收。

## 真实连接验证

| 检查 | 结果及边界 |
| --- | --- |
| 管理 API 的模型连通性测试 | HTTP 200，返回 `PONG` |
| Windows Chrome 中点击指定模型的“测试连通性” | 实际发出请求，页面显示成功及 `PONG`；未测试其他模型 |
| 统一模型执行器的 JSON 请求 | 返回符合诊断要求的 JSON，`ok=true`、`action=WAIT`；这是格式测试，不是交易决策 |
| 思考强度 | 配置和请求均为 `high`，服务未返回 reasoning 内容；不能由此证明服务端实际推理强度 |
| OKX 余额 | 1 个账户结果，有权益；报告不记录余额金额 |
| OKX 持仓、挂单 | 私有接口成功，均为 0；属于正常空结果 |
| OKX 账单 | 成功读取 4 条；未把账单条数当作成交数 |
| 保护单只读查询 | BTC SWAP 的 OCO / conditional 查询成功，当前无保护单；没有创建或测试触发保护单 |
| 公共行情 | BTC、ETH、SOL、DOGE、SUI、LINK 的 SWAP ticker 均返回有效价格 |
| K 线 | BTC SWAP 返回 10 条 |
| 修改后的首页采集函数 | 在新 Python 进程中使用真实模拟盘数据单次执行，账户可读、环境为 demo、数据健康状态 LIVE；没有模拟网络响应 |
| 本金未确认 | 余额正常展示；累计盈亏、ROI 和基准收益曲线暂不展示，未替用户设置本金 |

`LIVE` 在 `data_health.status` 中表示数据源健康/实时，**不是 OKX 实盘模式**。交易环境通过单独的 `okx_environment` 标识。

## 本轮新增发现与修复

1. **多供应商模型混用**：模型表原先仅按模型名去重，激活、测试、删除未完整使用供应商 ID。改为供应商和模型的联合身份；前端同时传递供应商 ID，并修复“主脑生效”标识。
2. **旧地址 / 旧密钥残留**：扁平模型缓存会覆盖刚修改的供应商配置。供应商本地定义现在作为权威来源；无密钥模型不得借用其他供应商的环境密钥。
3. **错误的开关与删除语义**：默认供应商被加载函数强制重新启用；清空供应商可能移除当前模型。修复开关持久化，保护当前供应商及模型，冲突请求返回可处理的 400。
4. **激活失败却已切换**：密钥存储失败时仍可能先修改活动模型。改为先保存凭据，再保存活动选择；不再由激活操作把 LLM 主密钥额外写入明文 `.env`。
5. **Windows Worker 重复失败**：监督器不断启动依赖 POSIX 的 Worker 后失败。Windows 管理服务不再启动该 Worker，并在后台说明运行边界；未替换或绕过 `fcntl` 锁。
6. **有模拟盘余额但首页无法读取**：首页强制依赖 CLI。增加明确的账户只读 REST 资源白名单；有 API Key 时直接读取，OAuth-only 配置保留原 CLI 路径。一次采集使用同一份不可变环境/凭据快照，失败不切换 LIVE。
7. **查看页面会触发账本同步**：首页刷新会启动账本同步脚本，而该脚本可能改写账本并产生通知。移除监控刷新中的主动同步；交易/日报 Worker 中已有的同步入口保留。
8. **缓存与健康状态不一致**：前端只读旧 `is_stale` 字段，忽略后端 `data_health`。现在识别 STALE/PARTIAL/OFFLINE；读取失败时不能复用其他环境或其他账户的缓存。
9. **未设定本金却显示累计收益**：默认 10,000 被当作用户确认的本金。新增确认状态，未确认时不展示基于默认值的累计盈亏及 ROI；余额和真实账单仍可读取。
10. **把模拟盘或未运行任务写成实盘 / 正在运行**：改正固定“实盘持仓”、默认“扫描中”和“每 6 小时自动”等文案；移除未核实的“100% OCO 覆盖”及固定委员会 ACTIVE 标识。
11. **真实成功状态对比度不足**：实际模型测试成功后，“状态: 200”被 `opacity-75` 再次淡化。移除透明度并增加前端回归。

## 页面与回归覆盖

- Windows 本机 Playwright + 有界面 Chrome，访问原有真实 `8080` 服务。
- 前台 6 页、管理后台 16 页，各检查亮/暗模式，44 个基础状态；补充窄屏模型列表、亮/暗真实测试结果及交互主题切换，共 48 个最终状态。最终报告中接口/JavaScript 错误 0、对比度问题 0、整页横向溢出 0、实际 body 主题不匹配 0。
- 审查辅助脚本将检查标签页置前并等待有限动画结束，避免把后台标签页尚未完成的颜色过渡误判成最终样式；没有通过覆盖应用颜色来消除问题。
- 检查依据：实际点击、DOM、计算样式、控件命名、主题匹配、横向溢出和接口/JavaScript 错误。截图留存；当前会话不支持图像输入，不宣称人工逐像素审查。
- 完整隔离后端回归：204 项通过（POSIX 测试环境，未复制运行数据和密钥）。
- 前端单元测试：18 项通过；严格类型检查与生产构建通过。
- 本轮没有重启原后端，因此浏览器巡检证明的是部署中页面的状态；新后端行为通过隔离回归和新进程中的真实只读采集分别验证。重启后的前后端整体复验仍待完成。

## 凭据与操作边界

- 密钥通过隐藏输入传入，仅用于用户指定的本地服务、模型供应商和 OKX 接口；不写入报告、源码或命令行参数。
- `.env` 仅保存非秘密设置；本次凭据未出现在待提交/未忽略文件或 `.env` 中。
- OKX 凭据与 LLM 运行时主密钥位于原有加密库 `data/r20_secrets.enc`。供应商配置仍沿用项目原有的私密 JSON 格式 `data/llm_models.json`，其中可能含明文供应商密钥；**Git 忽略不等于文件已加密**，不要公开整个 `data` 目录或本机截图。
- 没有下单、撤单、平仓、创建保护单、启动交易策略、开启模型委员会、保存通知配置、备份恢复或创建用户。
- 本金基准文件未创建，原账本文件未修改。只读诊断的首页缓存写到临时目录，不覆盖用户持久缓存。
- 结束时移除检查页面的请求拦截，浏览器及用户的其他标签页保持打开。

## 尚未通过完整实测的项目

1. **修复版本在原服务上的整体联动**：需要用户重启当前 Windows `8080` 后端，再复验首页余额、模拟盘标识、连接诊断和网关提示。
2. **自动交易与调度**：当前 Windows Worker 不能执行，Windows 服务未找到 OKX CLI。完整执行应在 WSL/Linux 准备依赖后单独验证；本轮未迁移或启动。
3. **模拟盘交易闭环**：下单、撤单、平仓、OCO 创建/触发、持仓变化和策略调度未实测，不能以空持仓读取成功代替。
4. **外部通知、备份恢复、多模型委员会、自进化长周期**：缺少相应配置或涉及写操作，未做真实端到端执行。
5. **全量数据组合和所有浏览器**：本轮不作覆盖保证。

## 本机证据与复现

报告和截图均被 Git 忽略：`frontend/.ui-artifacts/integration-review/`。

- `live-readonly-with-model.json`：包含结构化模型请求的第一轮真实检查。
- `live-readonly.json`：后续 14 项真实只读检查。
- `dashboard-final.json`：最终源码的单次真实首页采集验证。
- `ui-model-test.json`：浏览器实际点击模型测试的脱敏结果。
- `browser/report.json`：页面状态报告与同目录截图。

显式进行一次只读验证：

```powershell
python scripts/check_live_readonly.py --confirm-demo-readonly
```

加 `--model-json-test` 会额外发出一次可能计费的模型请求。该脚本拒绝实盘配置，不提供下单或策略启动选项。
