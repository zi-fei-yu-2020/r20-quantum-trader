# 移动端页面宽度回归

## 根因与修复

交易记录只有在有数据时才渲染宽表格。外层 `.terminal-grid` 未定义列轨道，默认的自动最小宽度会按表格 min-content 尺寸计算；其内部 `overflow-x-auto` 因父级已经被撑宽而失效。390px 浏览器中的普通交易数据曾将文档撑至约 1400px，长策略/原因字段可进一步放大。

单列内容轨道现在明确为 `minmax(0, 1fr)`，直接子项允许收缩；桌面双栏保留原比例。表格继续保留八列和全部原始字段，只有带可访问名称的表格区域横向/纵向滚动，键盘也可滚动。不在 body 上裁剪溢出来掩盖问题，不缩小整页或删除字段。

交易策略、平仓原因设定局部最大宽度并允许长单词换行；日志和长记忆文本允许换行。策略复盘标题/模型信息允许换行排列，防止同类长内容影响页面。

## 验证范围

Windows 桌面 Edge/Playwright 使用含记录、长连续英文标识、长策略描述及日志的隔离测试数据，覆盖 6 个前台页面、16 个管理页面及登录页；双主题、320/390/768/1440 宽度，另检查模型配置、模型列表、编辑弹窗和交易空状态。

除文档宽度外，还检查可见内容边界；交易表格能独立滚动到最后一列且不带动文档横移，筛选/搜索仍正确。代码回归测试位于 `frontend/tests/mobile-layout.test.mjs`，浏览器审查截图和报告位于本地 `frontend/.ui-artifacts/mobile-overflow/`。

后台使用隔离数据，未写入服务器配置或交易数据；生产观察面板另外进行只读验证。截图并非本会话逐像素视觉验收的替代品，检查结果以浏览器布局度量与交互断言为准。
