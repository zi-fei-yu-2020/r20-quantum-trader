# R20 Quantum Trader Docker ????

????????? Docker Compose ?? R20 Quantum Trader?

## ????

- ?? Linux ??? / VPS
- ??????? Python ? Node.js ????
- ?? Named Volumes ???????????? OKX ????
- ??????????? Caddy?Nginx ? Traefik??? HTTPS

---

## ????

### 1. ??????

```bash
cp env.example .env
chmod 600 .env
```

?? `.env` ????????

```dotenv
# LLM ??????
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=your_llm_api_key_here
LLM_MODEL=gemini-3.7-flash-high
LLM_REASONING_EFFORT=high

# OKX ?????demo ? live?
R20_OKX_ENV=demo
OKX_DEMO_API_KEY=
OKX_DEMO_SECRET_KEY=
OKX_DEMO_PASSPHRASE=

# ????????? Token
R20_SETUP_TOKEN=replace_with_a_long_random_setup_token
```

### 2. ???????

```bash
docker compose build
docker compose up -d
```

### 3. ??????

```bash
docker compose ps
docker compose logs -f --tail=100 app
```

- **??????**?`http://your-server-ip:8080/`
- **????**?`http://your-server-ip:8080/docs`
- **?????**?`http://your-server-ip:8080/admin`

---

## ??????

Compose ??????????????????????????????????

| ??? | ?????? | ?? |
|---|---|---|
| `r20_config` | `/app/config` | ?? `.env` ????????????????? |
| `r20_data` | `/app/data` | SQLite ?????????????????? |
| `r20_logs` | `/app/logs` | ????????????? |
| `r20_backups` | `/app/backups` | ????????? |
| `r20_okx` | `/home/r20/.okx` | OKX CLI ???? |
| `r20_bypy` | `/home/r20/.bypy` | ??????????? |
| `r20_npm` | `/home/r20/.npm-global` | ?? npm ???? |

---

## ?????????

Docker ?????????????? Git ???????????? 409?????????????

```bash
git pull origin dev
docker compose build --pull
docker compose up -d
```
